import renderer from "react-test-renderer";
import Users from "../components/classComponents/Users";

// first do - npm i react-test-renderer

// test("class component method testing", () => {
//     const componentData = renderer.create(<Users/>).getInstance();
//     expect(componentData.getUserLists()).toMatch("User List");
// })
test("class component method testing", () => {
   
})