import { render, screen } from "@testing-library/react";

import App from "../App";

test("test negative cases", async() => {
    // render(<App/>);
    // // const button = screen.getByText("Login");
    // const button = await screen.findByText("Login", {}, {timeout: 5000});
    // expect(button).toBeInTheDocument();
});