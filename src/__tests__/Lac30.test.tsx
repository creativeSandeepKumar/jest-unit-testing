import { render, screen } from "@testing-library/react";

import App from "../App";


test("change test id", () => {
    // render(<App/>);
    // const input = screen.getByRole("textbox");

    // expect(input).toBeInTheDocument();
    // expect(input).toHaveValue();
    // expect(input).toHaveValue("Sandeep");
    // expect(input).toHaveAttribute("id");
    // expect(input).toHaveAttribute("data-test");
    // expect(input).toHaveClass("test-style")
    // expect(input).toBeEnabled()
    // expect(input).toBeDisabled()

    //     for(let i = 0; i < alttg.length; i++){
    //     expect(alttg[i]).toBeInTheDocument();
    //     // expect(h1tag[i]).toHaveValue("Sandeep");
    // }

});
// test("test negative cases", () => {
//     render(<App/>);
//     const btn = screen.getByRole("button");

//     expect(btn).toBeInTheDocument();
//     expect(btn).not.toBeDisabled();
//     expect(btn).not.toHaveClass("btn");
//     expect(btn).not.toHaveAttribute("name");


// });