import { render, screen } from "@testing-library/react";

import App from "../App";

test("test negative cases", () => {
    // render(<App/>);
    // const texts = screen.getByText((content, element) => content.startsWith("Hello"));
    // const texts = screen.getByText((content, element) => content.endsWith("World"));
    // const texts = screen.getByText((content, element) => content.endsWith("ld"));
    // const texts = screen.getByText((content, element) => content.includes("ld"));
    // const texts = screen.getByText((content, element) => {
    //     return content.length == 11;
    // });


    // expect(texts).toBeInTheDocument();
});